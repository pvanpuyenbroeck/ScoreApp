import React, { useState } from 'react';
import classes from './ShowMore.css';

const ShowMore = (props) => {
    return (
      <div className={classes.Container}> textInComponent </div>
    );
}

export default ShowMore;
